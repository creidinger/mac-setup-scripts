# mac-setup-scripts
Setup Scripts for new mac computers

## Setup
Base computer image for Dev
Homebrew must be installed for this to work properly. 

Homebrew requires some permissions for pkgconfig
sudo chown -R $(whoami) /usr/local/lib/pkgconfig
chmod u+w /usr/local/lib/pkgconfig

## .Terminal config
Follow the instructions here:
https://github.com/creidinger/bash-profile-terminal-theme
